name: Telegram Bot Cloud Runner

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install python-telegram-bot==20.3 requests httpx

      - name: Run Bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          DATA_REPO: ${{ secrets.DATA_REPO }}
          DB_FILE: "db.json"
        run: |
          if [ -z "$BOT_TOKEN" ]; then echo "❌ BOT_TOKEN missing"; exit 1; fi
          if [ -z "$GH_TOKEN" ]; then echo "❌ GH_TOKEN missing"; exit 1; fi
          if [ -z "$DATA_REPO" ]; then echo "❌ DATA_REPO missing"; exit 1; fi
          
          echo "🚀 البدء في تشغيل البوت..."
          python bot_main.py
